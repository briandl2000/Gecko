#pragma once

#if defined(GECKO_PLATFORM_LINUX)

// Linux backend for gecko::platform threading queries. Implementations
// live in threading_linux.cpp; this header is reserved for any helpers
// that need to be shared across multiple Linux .cpp files.

#endif  // GECKO_PLATFORM_LINUX
