# platform_example

The interactive platform showcase. It retains the original multi-window
controls, window modes and decorations, title-bar buttons, constraints, input
snapshots, DPI/native-handle information, and event logging.

Run it and press `H` for the complete control list. Wayland is selected by
default when available; pass engine configuration changes in `App.cpp` to force
X11 while experimenting.
