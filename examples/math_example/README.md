# math_example

The original tour of vectors, matrices, camera transforms, AABBs, rectangles,
quaternions, and interpolation helpers. It remains a small terminal program and
uses Gecko's own formatter/terminal output instead of the C++ standard library.
