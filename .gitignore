# Build outputs
/out/

# Compilation database
compile_commands.json
.cache/

# IDE (user-specific)
.vscode/
.idea/
*.swp
*.swo
*~

# Local-only dev scripts (Pi cross-compile stays on the developer's machine)
scripts/copy_debug.py
scripts/aarch64-linux-toolchain.cmake

# Safety nets — we don't vendor any of these; system Vulkan SDK is a
# prerequisite (see docs/build.md). Blocks accidental check-ins.
third_party/prebuilt/
third_party/vulkan-headers/

# OS
.DS_Store
Thumbs.db

# Python bytecode (for scripts/)
__pycache__/
*.pyc

# Logs and working files
*.log
log.txt
gecko_trace.json
working_dir/

# direnv (if used)
.envrc
.direnv/
